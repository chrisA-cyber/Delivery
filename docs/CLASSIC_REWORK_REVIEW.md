# Classic rework adversarial review

Reviewed September 5, 2026, on `codex/classic-rework`, against baseline `97ed5d3` and the current uncommitted integration. This was an independent code and screenshot review; no credentials were read and no external data was changed. Scope included authorization, content intensity, historical references, Daily/challenges, retry behavior, recorder cleanup, Scribe/scoring provenance, and mobile/result presentation.

## Findings and remediation

| Initial finding | Severity | Current status and evidence |
| --- | --- | --- |
| Small packs permanently exhausted the eight-line recent exclusion window. Every four-line Clean pack failed on draw five; result-stage retry submitted the same exhausted exclusions. | P2 | **Addressed.** `GameExperience.nextPrompt` retries `NO_PROMPTS` once with only the current prompt excluded, retaining the same pack, audience, entitlement, and energy filters. The reviewer independently reproduced the original exhaustion against all six bundled packs and inspected the fix. The integration owner reports eight consecutive browser draws, no immediate repeats, and four unique first-cycle lines; `e2e/classic-rework.spec.ts` contains that regression. |
| Entered/completed Mature challenge matchups bypassed the content preference and exposed verdicts/playable audio when revisited with Clean preferences. | P2 | **Addressed.** The challenge page passes prompt rating into `ChallengeMatchView`; its content gate precedes verdict/audio rendering and supplies the explicit content control. Hiding the view pauses playback, removes its source, and resets the media element. Independent rerun of `src/components/challenge/challenge-match-view.test.tsx`: **2 tests passed**, covering waiting/completed states, reveal, and cleanup on returning to Clean. |
| Mutable challenge admission preceded idempotency retrieval, so losing a successful HTTP response made the same-key retry fail as already entered. Daily retries after midnight could similarly reject a completed receipt. This was an existing defect exposed by the requested recovery flow. | P2 | **Addressed; independently re-reviewed.** The authenticated/guest scope and fingerprint now bind the complete validated request before canonical admission. Canonical content, rating, entitlement, Daily expiry, and one-entry gates remain inside every new operation. Completed challenge retries additionally recheck current participant/invite scope, assigned recipient, blocks, account deletion/restrictions, and challenge containment before returning the cached receipt. Independent focused rerun: **38 tests passed** across judge retry (16), challenge receipt access (5), canonical resolution (11), and idempotency (6). Tests cover lost responses, UTC rollover, changed fields/audio/account, current privacy restrictions, and rejection of new invalid/expired/already-entered attempts. |
| A preserved legacy Daily result shared a retired prompt as a new Classic round, which appeared recordable but failed submission. Challenge creation could silently substitute a different active line. | P2 | **Addressed in code.** `PlayPage` stops retired line/direction links before mounting capture. `ResultScreen` receives mode, shares `/daily` for Daily results, explains retirement, and suppresses new challenge creation for retired assignments. The original receipt remains intact. The historical-Daily browser regression is present in `e2e/classic-rework.spec.ts`; its execution was pending at this review checkpoint. |

## Evidence and limits

- All four initial findings are addressed in the reviewed implementation. The historical-Daily browser test and broader final integration outcomes should be recorded by the integration owner; this reviewer has independently rerun only the focused suites identified above.
- Independent retry command: `/Users/christopherassef/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node node_modules/vitest/vitest.mjs run src/lib/server/judge-retry.test.ts src/lib/server/challenge-receipt-access.test.ts src/lib/server/content-resolution.test.ts src/lib/server/idempotency.test.ts` — **4 files / 38 tests passed**, September 5, 2026.
- Inspected `after/prepare-320.png`, `after/result-390.png`, and `after/supporting/challenge-390.png` under `docs/evidence/classic-rework`, plus current recorder, result, sharing, and shell implementation. The current result denominator CSS corrects spacing visible in an earlier saved screenshot.
- No additional actionable privilege escalation or recorder resource leak was identified in the reviewed changes. This is a bounded review, not proof that the entire application is defect-free.
- Mature publication has separate API/share-read checks and database enforcement; the additive enum/content/private-boundary migrations require execution and pgTAP verification in a configured isolated database. They were not executed by this reviewer.
- The numerical weights and accuracy transcript source remain on the original scoring contract, with a separate rubric revision and supplemental Scribe provenance. Statistical equivalence, fairness, comedy quality, and human performance perception remain uncalibrated.
- The integration owner's `docs/evidence/classic-rework/scribe-live.json` records **one successful live Scribe v2 smoke test** on 2.525 seconds of locally synthesized neutral speech: four words, exact phrase match, word timestamps. This reviewer inspected that receipt; no second paid call was made. It does not verify OpenAI audio judging, human speech/acting, or Supabase persistence.
- Real microphones/devices, signed-in end-to-end persistence, distributed idempotency, production migrations, and live OpenAI scoring require the separately documented integration/release checks. Their absence is distinct from the local bugs above.
