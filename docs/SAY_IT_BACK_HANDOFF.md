# Say It Back — September 6, 2026

Delivery implementation on `codex/classic-railway`. Intended runtime is Railway project `321c764e-38fe-4ecb-98f2-39c8654d0956`, service `34d3b855-25f3-4eea-9438-c43974cef738`, environment `8470f7d3-49f4-41bc-b39e-65fc1e5a1e22`; Supabase `rcsopyxrotbbfaqikire`. Existing Netlify production and Clipping are separate. The owner authorized implementation, additive migrations, Git publication and this deployment; older per-step approval gates are historical.

Classic connected verification passed real Auth sessions, signed-in judged persistence, exact cached retry with one play charge, history across refresh and fresh sign-in, cross-account/guest rejection, and genuine 300-second playback-link expiry/recovery. See `evidence/classic-railway/connected-account-20260906.json`. Auth UI now explains failures, preserves return destinations, hides disabled social providers and supports existing-password sign-in. Test identities were provisioned as disposable SQL fixtures, then used real Auth APIs. Email signup currently hits the hosted email-send limit; working SMTP or a configured OAuth provider is needed for reliable new-account onboarding. No Auth security setting was relaxed.

Say It Back implements the real scene → camera-free recording → synchronized dub → matching → retry/save/challenge loop. Immutable catalog manifests, clip snapshots and versioned scores live in Supabase. Private audio stays in `delivery-audio`; app playback routes authorize every media request. Local replay appears before scoring; durable uploads and cached paid responses preserve recovery. Guest takes expire after 24 hours and can be claimed after sign-in with the original signed device cookie. Exact normalized performances of curated scripts inherit the script rating; deviations receive live moderation and mature scenes remain private. Explicit challenge consent controls sharing; unrelated recipients cannot inspect each other's recordings. Account erasure cleans Say audio before identity cascade; existing cleanup also drains expired guests.

Matching `say-match-v1.2`: 50% transcript word error rate, 30% phrase entry/exit timing, 20% phrase lengths/pauses. Known character-name spellings, numbers and unambiguous contraction expansions normalize consistently; other substitutions remain errors. One Whisper transcription, no automatic provider retries. PCM acoustic energy verifies quiet word edges independently of the reference; raw ASR timestamps and measured adjustments are retained. This fixes the observed Whisper first-word-at-zero error over real leading silence; noisy or unsupported boundaries receive words-only results. No reference prompt biases transcription. Missing timing is disclosed as words-only and excluded from full-score challenges/comparisons. No intonation/emotion, vocal identity, attractiveness or volume score. The deterministic scorer is reproducible for fixed transcript/timestamps; ASR itself may vary. Playback preserves performance timing and corrects only measured capture startup offset.

Media provenance and curation: `SAY_IT_BACK_MEDIA.md`; immutable manifest `src/lib/say-it-back/catalog.json`. Validate additions with `node scripts/import-say-clips.mjs`; apply with explicitly matching `--project` and configured service credentials. Existing versions cannot be overwritten. Source audio is removed in selected role intervals; background audio also drops there when stems are unavailable.

Live checkpoint: all ten scenes completed real upload, private replay and Whisper matching. Guest claiming, signed-in history, cross-account denial, silence rejection, cached retries, explicit challenges and recipient comparison passed. The synthetic timing control retained exact PCM with one second added silence (98 to68; timing100to0). A same-hash recipient pair scored98/98. One inaccurate British synthetic name was retained as an error and is being checked with a clearer fixture. Desktop browser reference play/pause/seek passed for all10; final replay-race fix and matchingv1.2 are awaiting final deployment verification. Provider/DB failure recovery paths have focused mocked tests, not an induced production provider outage. Shared testing budget is capped at $5, tracked centrally in `evidence/say-it-back/testing-budget.json`. Only the assigned coordinator dispatches paid requests; synthesis/transcription for media preparation ran locally for $0.

Owner microphone session after deployment: open `/say-it-back`, watch a short scene, enable the microphone, record and compare Original/Your take, pause/seek/replay, retry, sign in and reopen from Profile, then create a challenge and open it on a second device. Repeat once on a phone. Note device/browser and any audible echo, clipping, sync drift or confusing control. Browser automation and synthetic speech do not prove real-device behavior or enjoyment.
