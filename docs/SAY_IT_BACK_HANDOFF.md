# Say It Back — September 6, 2026

Delivery implementation on `codex/classic-railway`. Intended runtime is the isolated Delivery Railway app at https://delivery-production-0577.up.railway.app, using its isolated delivery-classic-test Supabase project. Existing Netlify production and Clipping are separate. The owner authorized implementation, additive migrations, Git publication and this deployment; older per-step approval gates are historical.

Classic connected verification passed real Auth sessions, signed-in judged persistence, exact cached retry with one play charge, history across refresh and fresh sign-in, cross-account/guest rejection, and genuine 300-second playback-link expiry/recovery. See `evidence/classic-railway/connected-account-20260906.json`. Auth UI now explains failures, preserves return destinations, hides disabled social providers and supports existing-password sign-in. Test identities were provisioned as disposable SQL fixtures, then used real Auth APIs. Email signup currently hits the hosted email-send limit; working SMTP or a configured OAuth provider is needed for reliable new-account onboarding. No Auth security setting was relaxed.

Say It Back implements the real scene → camera-free recording → synchronized dub → matching → retry/save/challenge loop. Immutable catalog manifests, clip snapshots and versioned scores live in Supabase. Private audio stays in `delivery-audio`; app playback routes authorize every media request. Local replay appears before scoring; durable uploads and cached paid responses preserve recovery. Guest takes expire after 24 hours and can be claimed after sign-in with the original signed device cookie. Exact normalized performances of curated scripts inherit the script rating; deviations receive live moderation and mature scenes remain private. Explicit challenge consent controls sharing; unrelated recipients cannot inspect each other's recordings. Account erasure cleans Say audio before identity cascade; existing cleanup also drains expired guests.

Matching `say-match-v1.2`: 50% transcript word error rate, 30% phrase entry/exit timing, 20% phrase lengths/pauses. Known character-name spellings, numbers and unambiguous contraction expansions normalize consistently; other substitutions remain errors. One Whisper transcription, no automatic provider retries. PCM acoustic energy verifies quiet word edges independently of the reference; raw ASR timestamps and measured adjustments are retained. This fixes the observed Whisper first-word-at-zero error over real leading silence; noisy or unsupported boundaries receive words-only results. No reference prompt biases transcription. Missing timing is disclosed as words-only and excluded from full-score challenges/comparisons. No intonation/emotion, vocal identity, attractiveness or volume score. The deterministic scorer is reproducible for fixed transcript/timestamps; ASR itself may vary. Playback preserves performance timing and corrects only measured capture startup offset.

Media provenance and curation: `SAY_IT_BACK_MEDIA.md`; immutable manifest `src/lib/say-it-back/catalog.json`. Validate additions with `node scripts/import-say-clips.mjs`; apply with explicitly matching `--project` and configured service credentials. Existing versions cannot be overwritten. Source audio is removed in selected role intervals; background audio also drops there when stems are unavailable. The short “You’re a jerk” voice-over now has accurate opening-shot wording in metadata version `v2-7ed551138939`; assets/cues are unchanged, and the prior version remains available for existing takes/challenges.

Live verification: all ten scenes completed real upload, private replay and Whisper matching with local stock synthetic speech. Guest claiming, signed-in history, cross-account denial, silence rejection before a provider call, cached retries, explicit challenges, recipient comparison and rematch passed. Adding exactly one second of silence to otherwise identical PCM reduced the v1.1 result from 98 to 68, with words unchanged at 100 and timing falling from 100 to 0. Current v1.2 same-hash creator/recipient recordings scored 98/98; changing “jerk” to “gem” reduced words from 100 to 80 (overall 77). The clear American Barbara fixture scored words 100, timing 90, rhythm 74, overall 92; the earlier mispronounced synthetic name remains an error in its historical record. Audio hashes, actual provider receipts and versioned recomputations are retained in `evidence/say-it-back/`.

The application code at `ee3063bde6e167500d44d61bc97872b2a3a49d31` passed build, lint and 288 tests (three explicit live-provider opt-ins skipped), and deployed successfully to the verified Railway app. Final browser checks verified the replay-after-end fix and found one content-setting omission in teen challenge reads. The narrow fix passed six focused tests (one new content-gate regression), lint and build, and deployed at `d2fdce64fba6825ce2e0cfe104a67acdfe58f5bf` to that same app. Live browser pause/seek/resume, Original/Friend’s take comparison, repeated ended replay, refresh and Clean/Spicy challenge gating passed. Desktop screenshots were inspected; no physical microphone or mobile viewport was available. Provider/DB failure recovery uses focused mocked tests; no real provider outage was induced.

Paid testing is closed: one new Classic judge request (one or at most two provider dispatches) and 18 serial Say transcription/judge requests, SDK retries disabled. Conservative combined upper bound is $1.675 of the $5 allowance (including a $0.05 conservative bound for one cleanup-worker check); actual account billing is unavailable. Free local synthesis and media preparation cost $0. Central reservations and a process lock prevented competing paid batches; cached retries cost no additional play. See `evidence/say-it-back/testing-budget.json`.

Publication stays on `codex/classic-railway`, [PR #2](https://github.com/chrisA-cyber/Delivery/pull/2), deployed to the verified Railway environment above. Main remains unmerged because it still feeds the separate existing Netlify production; neither that deployment nor Clipping was changed. All three disposable accounts were erased through the actual account API: all 19 associated recordings, attempts and four challenges are gone, with three completed erasure receipts. A further unjudged guest fixture verified physical expiration cleanup. No test credentials or private user fixtures are committed.

Retention is scheduled hourly through Railway function `cleanup-scheduler`, with no public domain, a 45-second timeout and no retries. It invokes the existing moderation/account/guest cleanup endpoint with a generated secret reference. One actual invocation exited successfully and independent SQL verified the expired guest row and private object were removed. Platform liveness remains `/play`; `/api/health` deliberately still checks full launch billing configuration and returns 503 because Stripe credentials/prices are absent. Paid subscriptions were outside this free gameplay assignment and remain unconfigured.

Owner microphone session after deployment: open `/say-it-back`, watch a short scene, enable the microphone, record and compare Original/Your take, pause/seek/replay, retry, sign in and reopen from Profile, then create a challenge and open it on a second device. Repeat once on a phone. Note device/browser and any audible echo, clipping, sync drift or confusing control. Browser automation and synthetic speech do not prove real-device behavior or enjoyment.
