"use client";
import { DeliveryFeed } from "@/components/social/delivery-feed";
/** Uses the real feed and its honest empty/unconfigured state. */
export function LandingFeed() {
  return <DeliveryFeed />;
}
